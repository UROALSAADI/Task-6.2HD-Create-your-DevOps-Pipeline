pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add your build steps here (Maven, Gradle, Docker)
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Add your test steps here (JUnit, Selenium)
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Add your deployment steps here (Docker Compose, AWS)
            }
        }
    }
}
